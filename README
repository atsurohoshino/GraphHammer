# GraphHammer projects

GraphHammer projects includes GraphHammer itself
and test executables to run GraphHammer analyses.

GraphHammer is a library for running different 
analysis on a graph. It's designed to be used on
a generic PC and clusters.

Current version is a ready to be used prototype 
implementation.
